int read_time_ms;
int num_reads;
float x_sum, x_square_sum;

void setup() {
  Serial.begin(9600);
}

void loop() {
  // read as much as possible within a set time interval (read_time_ms)
  
  // compute average and uncertainty of the read during this interval
  
  // print out results
}
